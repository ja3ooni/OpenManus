# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: "OpenManus Dashboards"
    orgId: 1
    folder: "OpenManus"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
